spring:
  application:
    name: store
stripe:
  apiUrl: https://api.stripe.com
  enabled: true
  timeout: 1000
  supported-currencies: USE,EUR,GBP
payment-gateway: stripe
mail:
  host: https://smtp.gmail.com
  port: 5761